services:
  - type: web
    name: sommelier-ia-api
    runtime: python
    rootDir: examenes/maching_learning_TP_vinos
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
